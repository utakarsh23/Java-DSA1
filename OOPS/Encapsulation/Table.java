package Encapsulation;

public class Table {

    /// public      - > class, package, subclass(same pkg), subclass(diff pkg), World(everywhere)

    /// protected   - > class, package, subclass(same pkg), subclass(diff pkg)

    /// no modifier - > class, package, subclass(same pkg)

    /// private     - > class
}
